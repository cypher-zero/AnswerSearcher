# Build artifacts
app/build/
build/
.gradle/
.gradle_bak_*/
.gradle_bak_*/

# Local config (may contain SDK paths / secrets)
local.properties

# Compiled binaries (not source)
*.apk

# IDE
.idea/
*.iml
.DS_Store
Thumbs.db

# Signing keystore (NEVER commit — contains the private signing key)
keystore/
*.keystore
